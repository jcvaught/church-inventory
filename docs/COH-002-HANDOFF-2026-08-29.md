# COH-002 Handoff — Core Authorization Hardening

**Status:** Ready for Claude review  
**Branch:** `codex/coh-002-core-authorization`  
**Review SHA:** `47ecd6dbbcda43827637ecbd0192d35d4b7cc69c`
**Deliverable path:** `docs/COH-002-HANDOFF-2026-08-29.md`  
**Base:** `b090923` (current `main` when branched; includes COH-001 and the counterpart-branch check)  
**Client-first commit:** `812f15edcfba920b2ed5a8a5cc96540b57e697ee`

Review the immutable review SHA above, not the moving branch tip. John has
authorized Claude to create a temporary detached worktree at that SHA and run
tests against it. Do not merge, push, or deploy as part of review.

## Scope delivered

1. **Active membership predicate.** `isMember` rejects an explicit
   `active:false`. Per the approved refinement at `3dda1f0`, legacy profiles
   without the field remain admitted via
   `userData().get('active', true) == true`. The Stage 0 production-field check
   is therefore an optional confirmation, not a gate.
2. **People Access containment.** Admins/managers retain full reads. Ordinary
   members may read only `accessPeople` docs linked to their uid and
   `accessRecords` whose referenced person doc is linked to their uid. The
   client no longer opens the three full People Access listeners for ordinary
   users; it queries their linked person docs and records only. Settings → My
   Compliance consumes those scoped arrays and is self-only.
3. **Trusted activity attribution.** Activity creates pin `performedBy` to the
   authenticated uid, `performedByName` to the user profile name, and
   `timestamp` to `request.time`, following the `shepherdAudit` pattern. Client
   writes use `serverTimestamp()`. Read and analytics paths normalize both new
   Firestore Timestamps and historical ISO strings during the compatibility
   period.
4. **Legacy rule removal.** Removed exactly the document and comment blocks for
   `tasks` and `maintenanceTickets`. `taskTemplates` and canonical `workItems`
   rules remain intact.

The branch history deliberately puts the role-aware client subscription change
before the restrictive rules change, preserving the required deployment order.

## Files changed

- `src/useFirestore.js`
- `src/App.jsx`
- `src/pages/ActivityLogPage.jsx`
- `firestore.rules`
- `functions/test/rules/core-collections.test.mjs`
- `docs/DATA_MODEL.md`
- `docs/CHANGELOG.md`

## Verification

- `npm run lint` — pass, 0 errors; 50 pre-existing warnings.
- `npm run build` — pass; 34 blog posts and 5 static pages prerendered;
  production bundle verification checked 28 JS chunks with 0 `jsxDEV`.
- `npm run test:rules` — pass, 33/33 tests.

The rules suite now covers explicit inactive denial and missing-field legacy
compatibility; activity actor/name/time spoofing; immutable audit rows;
self-only People Access document and query reads; admin/manager full access;
all four retired paths; and preservation of `taskTemplates` and `workItems`.

## Review focus

- Confirm ordinary-member People Access queries satisfy the rules without
  revealing unrelated people or records.
- Probe direct-SDK activity writes with spoofed uid, name, and client timestamp.
- Confirm inactive profiles are denied while missing-`active` legacy profiles
  retain access.
- Confirm all four legacy paths have no matching rules and the noncontiguous
  `taskTemplates` block remains live.
- Check the ISO-string/Firestore-Timestamp compatibility queries, especially
  pagination boundaries and the string lane's type upper bound.

## Decisions and exclusions

- D-1 and D-10 are implemented. D-9 remains unchanged: activity-log reads stay
  available to all active members.
- D-4 remains deferred and AC-07 remains substantially open by owner decision.
- D-2, D-3, D-5, D-6, D-7, and D-8 remain unanswered and out of scope.
- No production data was inspected for Stage 0 step 2; that confirmation is now
  optional because missing `active` intentionally defaults to true.
- No merge, push, Firebase deploy, production E2E run, or production mutation
  was performed.

## Response to review at `12abe7e`

- **H-1 / DEC-2026-005 complete:** the name pin remains. `logActivity` now
  derives `performedByName` centrally from `userProfile?.name` with no fallback;
  all internal and direct callers pass only uid and details, so no page can
  supply a divergent audit name. The E2E seeder uses one shared `accountName()`
  derivation for both Auth display name and Firestore profile name.
- **Required probe added:** a nameless active profile's serialized realistic
  payload (no `performedByName`) is denied by the rule. The Web SDK also rejects
  `undefined` before submission, so both normal-client and direct-SDK paths fail
  closed.
- **Accepted stale-session limitation documented:** because `useAuth` loads the
  profile once, an out-of-band name change can deny that session's audit writes
  until re-login. No current UI path triggers such a change.
- **M-1 addressed:** ordinary-member record listeners are bounded to 100 rows
  per linked person, and `DATA_MODEL.md` records the parent-document `get()` and
  its query-budget implication.
- **L-1 addressed:** the changelog now documents that ordinary users receive an
  empty `timeEntries` array because its rules were already admin/manager-only.
- **L-2 addressed:** `AI-WORKBOARD.md` now declares the actual expanded file
  scope, including central and review-fix files.
- **Name-pin decision:** retained per DEC-2026-005.
- **Verification repeated:** `node --check scripts/setup-e2e-tenant.mjs` passed;
  `npm run lint` passed with 0 errors and the same 50 known warnings;
  `npm run build` passed, including prerender and bundle verification; and
  `npm run test:rules` passed 33/33. Production E2E was not run because it
  remains separately owner-authorized under the execution plan.

## Reviewer Findings

- Critical:
- High:
- Medium:
- Low:
- Questions:
- Verdict: Changes requested | Approved with follow-up | Approved
